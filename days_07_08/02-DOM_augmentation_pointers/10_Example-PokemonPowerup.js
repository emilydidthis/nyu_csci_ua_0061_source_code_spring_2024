// get all '.tile' divs

// set up a click handler on all '.tile' divs
    // get a reference to the element with the 'power' class inside of this div

    // extract the current power value

    // increase power

    // update power

